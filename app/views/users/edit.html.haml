- provide(:title, 'Edit user')
%h1="Edit user"
= form_for(@user) do |f|
  = render :partial => 'form', :locals => { :f => f }
  = gravatar_for(@user)
  = link_to "change", 'http://gravatar.com/emails'
  .actions
    = f.submit "Update"